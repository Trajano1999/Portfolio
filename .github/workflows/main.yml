name: main

on:
  push:
    branches: [ "main" ]

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.2
      - name: Hello World
        run: echo "Hello World!"
      - name: Files
        run: ls -la
